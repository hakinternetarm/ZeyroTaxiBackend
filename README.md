# Taxi API

ASP.NET Core minimal API for taxi app. Contains authentication (phone-based), driver submission flow, and file storage.

Run:

- dotnet restore
- dotnet build
- dotnet run

Notes:
- Default DB is sqlite at `taxi.db` in project folder
- Configure SMTP and Storage in `appsettings.json`